a = [1,2,3,4,5]
b = [6,9,8,4,5]
print(dir(b))
print(b.sort())


print(b)
# basically an array
# remove clears specified item from the list
# pop removes index item from the list, if no index item is given it removes the last item from a list
# clear removes all items from the list
# copy copies all the items of a list
# index stores the index value of a specified item from a list
# reverse reverses the data of the list
# sort sorts the array in ascending order b.sort(reverse=True) for descending order
# count counts the number of times a specified item is present in a list